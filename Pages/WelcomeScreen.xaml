<UserControl x:Class="DormitoryPATDesktop.Pages.WelcomeScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DormitoryPATDesktop.Pages"
             mc:Ignorable="d">
    <Border Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Margin="15">
        <Grid Margin="20" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" 
                       Text="Добро пожаловать!" 
                       FontSize="28" 
                       FontWeight="SemiBold" 
                       Foreground="#5D6572" 
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>

            <!-- Информация о сотруднике -->
            <TextBlock Grid.Row="1" 
                       x:Name="txtWelcomeMessage" 
                       FontSize="18" 
                       Foreground="#616161"
                       HorizontalAlignment="Center"/>            
        </Grid>
    </Border>
</UserControl>